services:
  korrekturabzug:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./templates:/app/templates:ro
      - ./output:/app/output
    environment:
      - KORREKTUR_CONFIG=/app/config.yaml
    restart: unless-stopped
